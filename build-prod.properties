# Configuracion COMUN del perfil de produccion (todos los clientes).
# Los datos por cliente (dbSchema, clientReleaseName) viven en build-prod-<cliente>.properties
# (ver build-prod-client.properties.sample). Cliente por defecto: terdemol.
debug=false
